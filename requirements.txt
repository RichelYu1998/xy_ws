# 核心依赖 (FastAPI)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
playwright>=1.59.0
openpyxl>=3.1.2
pymysql>=1.1.0
pandas>=1.3.0

# 系统监控与运维
psutil>=5.9.0
prometheus_client>=0.17.0

# 数据验证 (FastAPI 内置支持)
pydantic>=2.0.0

# 文档生成
python-docx>=1.2.0

# CORS 跨域支持 (已启用)
fastapi-cors>=0.0.6

# 已移除的 Flask 依赖 (已替换为 FastAPI)
# flask>=2.0.0
# flask-restx>=1.1.0